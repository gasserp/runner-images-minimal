# Keep the build context minimal. NOTE: builds use images/ as the context
# (see Makefile), so Docker reads images/.dockerignore; this root file applies
# only to builds run with the repository root as context.
.git
.gitignore
tests/
Makefile
README.md
**/*.md
_work/
.env
*.token
